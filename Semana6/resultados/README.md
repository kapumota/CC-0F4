### Resultados generados

El directorio recibe artefactos producidos por ejecución.

Archivo principal:

```text
latest_run.json
```

No se incluye un resultado precomputado como evidencia científica.

Debe regenerarse desde el entorno donde se ejecute el cuaderno.

Modo offline:

```bash
CC0F4_RUN_REAL_TOOL_LLM=0 make execute-cuaderno6
```

Modo LLM real:

```bash
CC0F4_RUN_REAL_TOOL_LLM=1 make execute-cuaderno6
```
